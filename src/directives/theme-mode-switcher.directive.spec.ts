import { ThemeModeSwitcherDirective } from './theme-mode-switcher.directive';

describe('ThemeModeSwitcherDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeModeSwitcherDirective();
    expect(directive).toBeTruthy();
  });
});
